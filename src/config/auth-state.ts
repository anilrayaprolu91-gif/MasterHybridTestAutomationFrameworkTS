/** Paths for the saved browser auth-state files produced by the setup project. */
export const CUSTOMER_AUTH_FILE = 'playwright/.auth/customer.json' as const;
export const ADMIN_AUTH_FILE = 'playwright/.auth/admin.json' as const;
